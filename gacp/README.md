# Git add, commit, push

Simple alias you know the deal. 
Be mindful: Adds all diffs, `git add .`, in current directory. 
*(yes I used this alias for this repo)*
### Flag/Argument

- `-m/--message`:
  Optional commit message, ex: `gacp -m "My meaningful commit message"`